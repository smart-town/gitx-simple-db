export interface Issue {
    owner?: string,
    repo?: string,
    title?: string,
    number?: string,
}

export interface Table {
    id?: string,
    name?: string,
    info?: string,
    total?: number,
}